import React from 'react'

type Props = {}
const bo = 'border border-solid border-white'

const Carousel = (props: Props) => {
  return (
    <div className={`flex w-full h-[50%] mt-[5%] ${bo}`}>
      hello
    </div>
  )
}

export default Carousel