import React from 'react'

type Props = {}
const bo = 'border border-solid border-white';

const Experience = (props: Props) => {
  return (
    <div className={`${bo} flex flex-col max-w-[100vw] h-[500px]` }>
        hello
    </div>
  )
}

export default Experience